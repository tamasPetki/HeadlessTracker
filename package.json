{
  "name": "headless-tracker",
  "version": "0.13.2",
  "description": "Not financial advice. Portfolio data aggregation MCP server for crypto positions across exchanges, on-chain wallets, and prediction markets.",
  "type": "module",
  "module": "src/mcp/server.ts",
  "bin": {
    "headless-tracker": "./bin/headless-tracker.ts"
  },
  "scripts": {
    "start": "bun run src/mcp/server.ts",
    "setup": "bun run bin/headless-tracker.ts setup",
    "test": "bun test --timeout=15000",
    "test:watch": "bun test --watch --timeout=15000",
    "typecheck": "bun --bun tsc --noEmit",
    "build:apps": "bun run scripts/build-mcp-apps.ts",
    "prepublishOnly": "bun test --timeout=15000 && bun --bun tsc --noEmit && bun run build:apps"
  },
  "files": [
    "bin/",
    "src/",
    "dist/mcp-apps/",
    "tsconfig.json",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "portfolio",
    "crypto",
    "stocks",
    "polymarket",
    "bybit",
    "binance",
    "metamask",
    "ibkr",
    "claude",
    "ai-tools"
  ],
  "author": {
    "name": "Tomi (PietScarlet)",
    "url": "https://bulltrapp.com"
  },
  "homepage": "https://github.com/tamasPetki/HeadlessTracker",
  "repository": {
    "type": "git",
    "url": "https://github.com/tamasPetki/HeadlessTracker.git"
  },
  "license": "MIT",
  "engines": {
    "bun": ">=1.3.0"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.7.1",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@napi-rs/keyring": "^1.1.6",
    "bybit-api": "^4.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
